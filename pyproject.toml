[project]
name = "meg-toolbox"
version = "0.1.0"
description = "Tools for MEG analysis"
authors = [
    {name = "Sebastian Coleman"}
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["meg_toolbox*"]
